# Environment variables (contains API keys)
.env
.env.local

# Node modules
node_modules/

# Build output - INCLUDED for plugin distribution
# (plugins are copied as-is, no npm install/build on install)

# Local plugin settings
.claude/*.local.md
