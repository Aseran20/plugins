# Environment variables (contains API keys)
.env
.env.local

# Node modules
node_modules/

# Build output - but include MCP server dist for plugins
# (plugins are copied as-is, no build step on install)

# Local plugin settings
.claude/*.local.md

# OS files
.DS_Store
Thumbs.db
nul
